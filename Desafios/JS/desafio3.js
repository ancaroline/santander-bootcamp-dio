//ainda resolvendo

let lines = gets().split("\n");

let line = lines.shift().split(' ');
let soma = 

//TODO: Complete os espaços em branco com uma possível solução para o desafio

for(i=0;   ;i++){
  let transfor = parseInt(line[ ])
  soma = soma + ;
}
print(soma-3)


const tomadas = gets().split(" ")
const result = tomadas.map(num => parseInt(num)).reduce((ac, item) => ac + item)
console.log(result - (tomadas.length - 1))